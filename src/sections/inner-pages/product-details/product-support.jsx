const ProductSupport = (popps) => (
    <div className="w-100 flex justify-center">
      <span>Includes 1 year service warranty
      <br />
      <a href={popps.link}>Click here </a> to download menual</span> 
      
    </div>
  );

export default ProductSupport;